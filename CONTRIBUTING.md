# مشارکت در کلنگ-لینتر

از مشارکت شما در توسعهٔ کلنگ-لینتر سپاسگزاریم! این سند راهنمای کوتاهی برای مشارکت در این مخزن است.

## شبیه‌سازی و ساخت

```bash
git clone https://github.com/faralidev/kolang-linter.git
cd kolang-linter
go build -o kolang-linter .
```

## اجرای تست‌ها

```bash
go test ./...
```

## ساختار پروژه

- `main.go` — نقطهٔ ورود برنامه؛ خواندن کد کلنگ از فایل یا stdin و نوشتن خروجی JSON
- `internal/diag` — ساختار تشخیص (diagnostic) و سریال‌سازی JSON
- `internal/rules` — پیاده‌سازی قواعد لینت در سه دستهٔ نحوی، سبکی و معنایی

## چگونه مشارکت کنیم؟

- برای گزارش باگ یا پیشنهاد ویژگی، یک Issue باز کنید.
- برای تغییر کد، یک Pull Request ایجاد کنید.
- پیش از ارسال، مطمئن شوید همهٔ تست‌ها با `go test ./...` می‌گذرند.
- پیام‌های تشخیص‌ها باید فارسی و ساده باشند.

هر مشارکتی، هرچند کوچک، پذیرفته است. منتظر Pull Request شما هستیم!